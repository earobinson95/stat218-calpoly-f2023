---
title: "Chapter 7: Comparing a Numerical Variable Across Two Groups"
format: 
  html:
    toc: true
#   pdf:
#     keep-tex: false
#     include-in-header:
#       text: |
#           \usepackage{fancyhdr}
#           \pagestyle{fancy}
#           \fancyhf{}
#           \fancyhead[R]{Chapter 7: Comparing a Numerical Variable Across Two Groups}
#           \fancyfoot[C]{\thepage}
#   docx:
#     toc: false
# prefer-html: true
embed-resources: true
bibliography: references.bib
reference-location: section
execute:
  echo: false
  message: false
  warning: false
---

In this chapter, we will consider methods that allow us to make comparisons on numerical variables between two different groups.  In general, these methods should be used to address research questions involving a *categorical explanatory* variable (with two categories) and a *numerical response* variable.

Two different cases will be considered: 

(1) When the samples are independent, and 

(2) when the samples are dependent (i.e., matched or paired).

## COMPARING TWO POPULATION MEANS: INDEPENDENT SAMPLES (two-sample independent t-test)

The hypothesis testing procedures presented in this section should be used when the observations from the two groups being compared are *independent*. Whether or not the observations between the two groups are *independent* is determined by how the data are collected. To see this, consider the following example.

### Example 7.1: Nicotine withdrawal and Perceived Elapsed Time

```{r}
library(tidyverse)
library(mosaic)
library(infer)

set.seed(24)
nicotine <- tibble(status = "withdrawal",
                   sex = c(rep("male", 12), rep("female", 8)),
                   age = runif(20, min = 25, max = 50),
                   time_passed = rnorm(n = 20, mean = 55.05, sd = 9.32)
                   ) |> 
  sample(20) |> 
  select(-orig.id)

set.seed(24)
nonsmokers <- tibble(status = "no withdrawal",
                   sex = c(rep("male", 12), rep("female", 10)),
                   age = runif(22, min = 25, max = 50),
                   time_passed = rnorm(n = 22, mean = 46.85, sd = 9.35)
                   ) |> 
  sample(22) |> 
  select(-orig.id)


time_elapsed <- nicotine |> 
  bind_rows(nonsmokers) |> 
  mutate(age = round(age, 0),
         time_passed = round(time_passed, 2)) |> 
  sample(42) |> 
  select(-orig.id)
```

Recall the study conducted by researchers at Pennsylvania State University which investigated whether time perception, a simple indication of a person’s ability to concentrate, is impaired during nicotine withdrawal. This study was discussed in Chapter 6. Twenty smokers were put through a 24-hour smoking abstinence and were asked to estimate how much time had passed during a 45-second period. Then, we analyzed another 22 individuals who were daily smokers, but were not asked to abstain from smoking for 24-hours (i.e., no nicotine withdrawal). They were also asked to estimate how much time had passed during a 45-second period. Suppose the resulting data on perceived elapsed time (in seconds) were collected from both groups as shown below (these results are artificial but are similar to the actual findings). 

```{r}
#| echo: true
head(time_elapsed)
```

**Research Question**: Do daily smokers suffering from nicotine withdrawal tend to believe that more time has elapsed than daily smokers *not* suffering from nicotine withdrawal?

1. What is the observational unit for this study?

<span style="color:blue;">A daily smoker</span>

2. What are the variables assessed in this study? What are their roles (explanatory / response) and data types?

+ <span style="color:blue;">Explanatory: status (categorical) - withdrawal/no withdrawal</span>
+ <span style="color:blue;">Response: time passed (numerical) measured in seconds</span>

3. What are the parameters of interest for this study?

<span style="color:blue;">$\mu_\text{withdrawal}:$ the population mean perceived time passed for all daily smokers suffering nicotine withdrawal.</span>
<span style="color:blue;">$\mu_{\text{no withdrawal}}:$ the population mean perceived time passed for all daily smokers NOT suffering nicotine withdrawal.</span>

<span style="color:blue;">$\mu_\text{withdrawal}-\mu_{\text{no withdrawal}}:$ the difference in population mean perceived time passed between for all daily smokers suffering nicotine withdrawal and all daily smokers NOT suffering nicotine withdrawal.</span>

Similar to summarizing a single numeric variable, we can compare the two groups with summary statistics from the `favstats()` function.

```{r}
#| echo: true
favstats(time_passed ~ status, data = time_elapsed) # <1>
```

1. Using the `favstats` function, denote the `response ~ explanatory` variables from the `data = ` set.

\vspace{0.05in}

4. Report the observed mean perceived time passed for study participants suffering from nicotine withdrawal. How about for participants *not* suffering from nicotine withdrawal?

+ <span style="color:blue;">$\bar x_\text{withdrawal}=52.65$ seconds</span>
+ <span style="color:blue;">$\bar x_\text{no withdrawal}=45.22$ seconds</span>


5. Calculate the observed sample difference in the mean perceived time elapsed between participants suffering nicotine withdrawal and participants not suffering nicotine withdrawal.

+ <span style="color:blue;">$\bar x_\text{withdrawal} - \bar x_\text{no withdrawal}=52.65-45.22=7.43$ seconds</span>

<span style="color:blue;">We will use this to test claims (hypothesis testing) about the difference in the population means!</span>

**Facetted Histograms**

When we want to add a categorical variable (like withdrawal status) to a histogram, we can create separate plots for each level of the categorical variable. These separate plots are called facets. We are comparing the perceived time passed between participants suffering withdrawal and participants not suffering withdrawal, so we will have two facets, one per status.

```{r}
#| fig-height: 2.5
#| echo: true
ggplot(data = time_elapsed, 
       mapping = aes(x = time_passed)) + 
  geom_histogram(binwidth = 5, color = "white") + 
  labs(x = "Perceived Time Elapsed (seconds)",
       y = "Number of Participants") +
  facet_wrap(~ status)
```


**Side-by-Side Box Plots**

Another way we can incorporate a categorical into our plots is to plot our boxplots for each group side-by-side. As opposed to faceting, these boxplots will be on the **same** plot. We only need to add one extra piece to our code from the previous chapter: a categorical variable. 

```{r}
#| fig-height: 2
#| echo: true
ggplot(data = time_elapsed, 
       mapping = aes(x = time_passed, 
                     y = status)) +
  geom_boxplot() +
  labs(x = "Percieved Time Elapsed (seconds)", 
       y = "Status")
```

Now that we have explored our data with summary statistics and visualizations, we want to use our data to draw inferences and make claims about the larger population.

Since we know the difference will change from sample to sample, in order to make valid inferences about the true population difference, we must first understand how the difference in sample means is expected to change from sample to sample.  That is, we must determine what differences in means are likely to happen by chance when taking random samples from populations with the same mean.  

6. Set up the null and alternative hypotheses (in words and symbols).

<span style="color:blue;">Null: The difference in population mean perceived time passed between all daily smokers suffering nicotine withdrawal and all daily smokers NOT suffering nicotine withdrawal is equal to 0 (i.e., the population means are equal).</span>

<span style="color:blue;">Alternative: The difference in population mean perceived time passed between all daily smokers suffering nicotine withdrawal and all daily smokers NOT suffering nicotine withdrawal is greater than 0 (the population mean perceived time passed for withdrawal is greater than the population mean perceived time passed for no withdrawal).</span>

In order to test our research question, we could conduct a simulation similar to what we did with two categorical variables (e.g., Mythbusters yawn experiment). Recall, we follwed the following steps:

+ Step 1: Write the <span style="color:blue;">status</span> and <span style="color:blue;">time passed</span> on <span style="color:blue;">42</span> cards.

\vspace{0.1in}

+ Step 2: Simulate what could have happened if the null was true and <span style="color:blue;">rip apart the pairs of status and time passed.</span>

\vspace{0.1in}

+ Step 3: Generate a new data set by <span style="color:blue;">shuffling and randomly re-matching new simulated pairs (assuming no difference/relationship).</span>

\vspace{0.1in}

+ Step 4: Calculate the <span style="color:blue;">difference in means</span> for the new simulated data set and add it to the dot plot.

\vspace{0.1in}

We would then repeat this process 100 or 1000 times to get an idea of what the sampling distribution of the *difference* in means between the two groups looks like.

7. Assuming there is *no difference* in mean perceived time elapsed between participants suffering from nicotine withdrawal and participants not suffering from nicotine withdrawal (i.e., Null is true), where do you expect the distribution to be centered? Explain.

<span style="color:blue;">I would expect the difference to be 0 (centered) if the population means are the same!</span>

The plot below contains 19 panels of "new simulated" data sets under the assumption there is no difference in mean perceived time elapsed between the two groups (i.e., Null is true), following the process above, and one panel of the actual data observed from the sample of 42 daily smokers in our study.

```{r}
#| fig-height: 8
#| fig-width: 8
library(nullabor)
set.seed(56156)
lineup_data <- lineup(null_permute("status"), time_elapsed, pos = 14) 

lineup_data |> 
ggplot(mapping = aes(x = time_passed, 
                     y = status)) +
  geom_boxplot() +
  labs(title = "Simulate Data under assumption there is no difference in \nthe mean perceived time elapsed (i.e., null is true)",
       subtitle = "19 simulated data sets + 1 observed data set from study",
       x = "Perceived Time Elapsed (seconds)", 
       y = "Status") +
  theme(aspect.ratio = 1) +
  theme_bw() +
  facet_wrap(~ .sample)
```


8. Which panel contains the actual data observed in the sample of 42 daily smokers in our study? Was it hard to pick out? Remember what it was like trying to pick this out.

<span style="color:blue;">Panel 14 contains the OBSERVED data from our 42 daily smokers. All other panels contain boxplots displaying the null data sets simulated under the assumption the null is true (no difference). A little tricky to pick out, but not bad! Maybe there is something going on in our data.</span>

We could take these and calculate the observed difference in mean perceived elapsed time between the two groups for each panel and plot this to begin creating the distribution of our sampling difference in means.

```{r}
#| fig-height: 8
#| fig-width: 8
obs_diffs <- lineup_data |> 
  group_by(.sample, status) |> 
  reframe(mean = mean(time_passed)) |> 
  pivot_wider(id_cols = .sample,
              names_from = status,
              values_from = mean) |> 
  mutate(Diff_Means = round(`withdrawal` - `no withdrawal`, 2)) |>
  mutate(label = paste0(round(`withdrawal`, 2), "-", round(`no withdrawal`, 2), " = ", Diff_Means)) |> 
  mutate(plot_type = ifelse(.sample == 14, "target", "null"))

lineup_data |> 
ggplot() +
  geom_boxplot(aes(x = time_passed,
                   y = status),
               alpha = 0.2) +
  labs(title = "Calculate the difference in means",
       subtitle = "withdrawal - No withdrawal",
       x = "Percieved time elapsed (seconds)", 
       y = "Status") +
  facet_wrap(~ .sample) +
  theme(aspect.ratio = 1) +
  theme_bw() +
  geom_label(data = obs_diffs, aes(label = label, x = 50, y = 1.5, fill = plot_type), size = 3, color = "white", fontface = "bold", show.legend = F) +
  scale_fill_manual(values = c("gray", "orange3"))
```

<span style="color:blue;">recall $\bar x_\text{withdrawal} - \bar x_\text{no withdrawal}=7.43 seconds$ from Q5.</span>

```{r}
#| fig-align: center
#| fig-pos: H
set.seed(56156)
obs_diffs100 <- lineup(null_permute("status"), time_elapsed, pos = 1, n = 1001)  |> 
  group_by(.sample, status) |> 
  reframe(mean = mean(time_passed)) |> 
  pivot_wider(id_cols = .sample,
              names_from = status,
              values_from = mean) |> 
  mutate(Diff_Means = round(`withdrawal` - `no withdrawal`, 2)) |>
  mutate(label = paste0(round(`withdrawal`, 2), "-", round(`no withdrawal`, 2), " = ", Diff_Means)) |> 
  mutate(plot_type = ifelse(.sample == 1, "target", "null"))

obs_diffs100 |> 
  filter(plot_type == "null") |> 
  ggplot(aes(x = Diff_Means)) +
  geom_dotplot(method = "histodot",
               dotsize = 0.15,
               ) +
  theme_test() +
  labs(title = "Sampling Distribution for Difference in Means (under assumption no difference)",
       y = "",
       x = "Difference in mean percieved elapsed time (seconds) \n Withdrawal - No Withdrawal") +
  theme(axis.text.y = element_blank(),
        axis.ticks.y = element_blank())
```

9. Does our observed data appear to be unusual compared to the data simulated under the assumption there is no difference?

<span style="color:blue;">yes, our observed difference in means is much larger than what we would expect to see under the assumptin there is no difference (null true) --> p-value < 0.0001. Draw your line at $\bar x_\text{withdrawal} - \bar x_\text{no withdrawal}=7.43 seconds$!</span>

It turns out that these sample means vary in a predictable way, and we can use what we know about this predictable pattern to determine what outcomes are likely (or not likely) to happen by chance.  This predictable pattern is called the *sampling distribution for the difference in means*.

\newpage

::: callout-note
### Characteristics for Sampling Distribution for Difference in Means

When the samples are **independent**, the sampling distribution for the difference in means can be described as follows.

1.	mean = $\mu_1 – \mu_2$.

2.	standard deviation of the sampling distribution (i.e., standard error) = $$\sqrt{\frac{s_1^2}{n_1} + \frac{s_2^2}{n_2}}$$

*Note: we are assuming the variances are unequal in this calculation, there are other formulas for assuming equal variances.*

3. The shape of the sampling distribution is approximately normal if both sample sizes are “sufficiently large” (> 30 in each group) OR if both original populations are normally distributed.

Given these characteristics, the test statistic we will use when testing for differences in two population means for independent samples is as follows:

$$T= \frac{\text{observed value}-\text{null value}}{\text{standard error}}= \frac{(\bar x_1 - \bar x_2)-(\mu_1 - \mu_2)}{\sqrt{\frac{s_1^2}{n_1} + \frac{s_2^2}{n_2}}}$$

These test statistics both follow a t-distribution, and the degrees of freedom are given by $(n_1 - 1) +(n_2 - 1)$. 

*Note: there are more complicated ways to adjust for these degrees of freedom, but that is beyond the scope of this class. You may notice that R outputs a different number of degrees of freedom than what this formula gives you.*
:::

10. Calculate the observed T test-statistic for the study data.

<span style="color:blue;">$$T = \frac{7.43 - 0}{\sqrt{\frac{7.27^2}{20}+\frac{6.83^2}{22}}}=3.41$$</span>

\newpage

11. Using the t-distribution provided below, show how you would estimate the p-value.

```{r}
#| out-width: 70%
#| fig-align: center
#| fig-pos: "H"
knitr::include_graphics("07-images/tdist-7.1.png")
```

<span style="color:blue;">Draw your line at T = 3.41 from above and recall $H_A: \mu_\text{withdrawal} > \mu_\text{no withdrawal}$ (aka $H_A: \mu_\text{withdrawal} - \mu_\text{no withdrawal}>0$) so this is an upper tailed test _. p-value is very small!!!</span>

Additionally, we can use R to conduct our two-sample independent t-test to answer our research question.

```{r}
#| echo: true
t_test(x = time_elapsed, # <1>
       explanatory = status, # <2>
       response = time_passed, # <3>
       order = c("withdrawal", "no withdrawal"),
       #mu = 0, # <4>
       alternative = "greater", # <5>
       conf_int = FALSE) # <6>
```

1. Using the `t_test()` function, designate the `x = ` data set.
2. Then identify the `explanatory = ` variable,
3. and the `response = ` variable.
4. The `order = c("Group 1","Group 2")` we are testing is $\mu_1 - \mu_2$,
5. with an `alternative = ` upper tailed test.
6. Let's hide the confidence interval, `conf_int = FALSE`, output for now.

\vspace{0.05in}

12. Write the conclusion in context of the problem. Make sure to include your evidence.

<span style="color:blue;">At an $\alpha=0.05$, we have evidence to conclude the difference in population mean perceived time passed between all daily smokers suffering nicotine withdrawal and all daily smokers not suffering nicotine withdrawal is greater than 0 (T = 3.41, df = 39, p = 0.0007).</span>

::: callout-note
## Conditions for two-sample independent t-test

1.	Independent observations *between* groups.
2.  Independent observations *within* groups.
3.  Sufficiently large sample sizes in both groups (i.e., $n_1>30$ and $n_2>30$). If not, is it reasonable to assume that both populations are normally distributed?

:::

13. Check the conditions for using the two-sample independent t-test.

+ <span style="color:blue;">Independent observations between groups: the perceived time passed for a daily smoker in the withdrawal group does not tell us anything about the perceived time passed for a daily smoker in the no withdrawal group.</span>
+ <span style="color:blue;">Independent observations within groups: the perceived time passed for a daily smoker in the withdrawal group does not tell us anything about the perceived time passed for a daily smoker in the withdrawal group. (similar for no withdrawal)</span>
+ <span style="color:blue;">Large enough sample size: $n_\text{withdrawal} = 20 < 30$ and $n_\text{no withdrawal} = 22 < 30$. Looking at the histograms of the observed data, these are approximately normal!</span>

14. Can we say that nicotine withdrawal *causes* a longer perceived elapsed time? Explain.

<span style="color:blue;">We would need more information about how participants were assigned to groups. If withdrawal status was *randomly assigned* to participants, then we balance out the effect of confounding variables and can conclude causation.</span>

### Example 7.2: Diet and Cholesterol (Two-sample Independent t-test)

Researchers investigated whether eating corn flakes compared to oat bran had an effect on serum cholesterol levels. Twenty-eight (28) adults were randomly assigned a diet that included either corn flakes (14 individuals) or oat bran (14 individuals). After two weeks, cholesterol levels (mmol/L) of the participant were recorded.

```{r}
cholesterol_data_long <- read_csv("data/cholesterol_data_long.csv")
```

```{r}
#| echo: true
head(cholesterol_data_long)
```

**Research Question**: Does eating corn flakes compared to oat bran have an effect on serum cholesterol levels? In other words, is there a difference in mean cholesterol levels between all adults on a corn-flake diet compared to all adults on an oat bran diet?

The summary statistics and visualizations for the study are provided below:

```{r}
#| echo: true
favstats(Cholesterol ~ Diet, data = cholesterol_data_long)
```

```{r facet-preview}
#| fig-height: 2.5
#| fig-align: center
ggplot(data = cholesterol_data_long, 
       mapping = aes(x = Cholesterol)) + 
  geom_histogram(binwidth = 0.75, color = "white") + 
  labs(x = "Cholesterol Level (mmol/L)",
       y = "Number of Participants") +
  facet_wrap(~ Diet)
```

```{r vertical-box}
#| fig-height: 2
#| fig-align: center
ggplot(data = cholesterol_data_long, 
       mapping = aes(x = Cholesterol, 
                     y = Diet)) +
  geom_boxplot() +
  labs(x = "Cholesterol Level (mmol/L)", 
       y = "Diet")
```

1. What is the observational unit for this study?

<span style="color:blue;">An adult</span>

2. What are the variables assessed in this study? What are their roles (explanatory / response) and data types?

+ <span style="color:blue;">Explanatory: Diet (cornflake / oatbran) - categorical </span>
+ <span style="color:blue;">Response: Cholesterol (mmol/L) - numerical</span>

3. What are the parameters of interest for this study?

+ <span style="color:blue;">$\mu_\text{Cornlfakes} - \mu_\text{oatbran}$: the difference in population mean cholesterol between the cornflake diet and the oatbran diet for all adults.</span>

4. Report the observed sample mean cholesterol level for study participants on the corn flake diet. How about on the oat bran diet?

+ <span style="color:blue;">$\bar x_\text{cornflake}=4.44$ mmol/L</span>
+ <span style="color:blue;">$\bar x_\text{oatbran}=4.08$ mmol/L</span>

5. Calculate the observed difference in the mean cholesterol level between participants on the corn flake diet and participants on the oat bran diet.

<span style="color:blue;">$\bar x_\text{cornflake}-\bar x_\text{oatbran}=4.44-4.08=0.36$ mmol/L</span>

6. Set up the null and alternative hypotheses, *in symbols*.

+ <span style="color:blue;">$H_0: \bar \mu_\text{cornflake}-\bar \mu_\text{oatbran}=0$</span>
+ <span style="color:blue;">$H_0: \bar \mu_\text{cornflake}-\bar \mu_\text{oatbran}\ne0$</span>

7. Check the conditions for using the two-sample independent t-test.

+ Independent observations *within* groups: <span style="color:blue;">The cholesterol of an adult on the cornflake diet doesn't influence the cholesterol of another adult on the cornflake diet (and same for oatbran diet).</span>

\vspace{0.1in}

+ Independent observations *between* groups: <span style="color:blue;">The cholesterol of an adult on the cornflake diet doesn't influence the cholesterol of another adult on the oatbran diet.</span>

\vspace{0.1in}

+ Normality assumption: <span style="color:blue;">$n_\text{cornflake}=14<30$ and $n_\text{oatbran}=14<30$. Thus, look at histograms of observed cholesterol. The histograms of the observed cholesterol for both diets are appoximately normal/bell shaped.</span>

\vspace{0.1in}

8. What do we know about the sampling distribution of the difference in means?

+ Shape: <span style="color:blue;">Approximately normal</span>

\vspace{0.1in}

+ Mean: <span style="color:blue;">0 (if no difference (i.e., null is true) we expect $\mu_\text{cornflake}-\mu_\text{oatbran}=0$</span>

\vspace{0.1in}

+ Standard Deviation (i.e., Standard Error): <span style="color:blue;">$\sqrt{\frac{0.97^2}{14}+\frac{1.05^2}{14}}=0.38$mmol/L</span>

\vspace{0.1in}

9. Calculate the observed T test-statistic for the data in the study.

<span style="color:blue;">$$T=\frac{0.36-0}{0.38}=0.95$$</span>

10. Using the t-distribution provided below, show how you would estimate the p-value.

```{r}
#| out-width: 65%
#| fig-align: center
knitr::include_graphics("07-images/tdist-7.2.png")
```

<span style="color:blue;">Draw a line at -0.95 and +0.95 and shade the tailends from there. p-value "looks like" about 0.3.</span>

\newpage

Alternatively, we could conduct the two-sample independent t-test via R.

```{r}
#| echo: true
t_test(x = cholesterol_data_long,
       explanatory = Diet,
       response = Cholesterol,
       order = c("CORNFLK", "OATBRAN"),
       alternative = "two-sided",
       conf_int = FALSE)
```

11. Using the output above, write a conclusion in context of the study. Make sure to include your evidence.

<span style="color:blue;">At an $\alpha = 0.05$, we do not have enough evidence to conclude the difference in population mean cholesterol levels for all adults on the cornflake diet compared to the oatbran diet is different from 0 mmol/L (T = 0.947, df = 25.8, p = 0.352)</span>

12. If we had found evidence of a difference in cholesterol levels between the two diets, could we say that cereal diet *causes* a change in cholesterol levels? Explain.

<span style="color:blue;">Yes, since diet was *randomly assigned* to subjects. Thus, it reduces the effect of confounding variables.</span>

::: callout-note
### Constructing a Confidence Interval for the Difference in Means 

$$ \text{point estimate} \pm \text{t-quantile} \times \text{SE}$$

$$(\bar x_1 - \bar x_2) \pm \text{t-quantile}\sqrt{\frac{s_1^2}{n_1} + \frac{s_2^2}{n_2}}$$
:::

13. Calculate the 95\% confidence interval for the study using the t-quantile provided below:

```{r}
#| echo: true
qt(0.975, df = 40)
```

+ Lower Endpoint =  <span style="color:blue;">0.36 - 2.02(0.38) = -0.41</span>

\vspace{0.5in}

+ Upper Endpoint =  <span style="color:blue;">0.36 + 2.02(0.38) = 1.12</span>

\vspace{1.2in}

14. Interpret the meaning of this 95% confidence interval in context of the study.

<span style="color:blue;">We are 95\% confident the difference in population mean cholesterol levels for all adults on the cornflake diet compared to the oatbran diet is between -0.41 and 1.12.</span>

alternatively...

<span style="color:blue;">We are 95\% confident the population mean cholesterol for all adults on the cornflake diet is between 0.41 lower and 1.12 higher than the population mean cholesterol for all adults on the oatbran diet.</span>

15. What does it mean for 0 to fall within the confidence interval?

<span style="color:blue;">0 is a plausible difference in population mean cholesterol levels. In other words, it is *plausible* the mean cholesterol levels are "the same". Recall we FTR the null.</span>

## COMPARING TWO POPULATION MEANS: DEPENDENT SAMPLES (paired t-test)

The hypothesis testing procedures presented in this section should be used when the observations from the two groups being compared are dependent. Whether or not the observations are dependent is determined by how the data are collected. To see this, consider the following example.

### Example 7.3: Diet and Cholesterol (Paired t-test)

```{r data-prep, echo = FALSE}

# https://www.sjsu.edu/faculty/gerstman/StatPrimer/paired.pdf

cholesterol_data <- tibble::tribble(
          ~ ID, ~CORNFLK, ~OATBRAN,
                                    "1", "4.61", "3.84",
                                    "2", "6.42", "5.57",
                                    "3", "5.40", "5.85",
                                    "4", "4.54", "4.80",
                                    "5", "3.98", "3.68",
                                    "6", "3.82", "2.96",
                                    "7", "5.01", "4.41",
                                    "8", "4.34", "3.72",
                                    "9", "3.80", "3.49",
                                   "10", "4.56", "3.84",
                                   "11", "5.35", "5.26",
                                   "12", "3.89", "3.73",
                                   "13", "2.25", "1.84",
                                   "14", "4.24", "4.14"
          )

factorCols <- c("ID")
cholesterol_data[,factorCols] <- lapply(cholesterol_data[,factorCols], as.factor)
numericCols <- c("CORNFLK", "OATBRAN")
cholesterol_data[,numericCols] <- lapply(cholesterol_data[,numericCols], as.numeric)

cholesterol_data <- cholesterol_data |> 
  mutate(CholesterolDiff = CORNFLK - OATBRAN)
```

Recall, in **Example 7.2**, researchers investigated whether eating corn flakes compared to oat bran had an effect on serum cholesterol levels. Twenty-eight (28) individuals were randomly assigned a diet that included either corn flakes (14 individuals) or oat bran (14 individuals). After two weeks, cholesterol levels (mmol/L) of the participant were recorded.

***But actually what happened was…***

Fourteen (14) individuals were randomly assigned a diet that included either corn flakes or oat bran. After two weeks on the initial diet, serum cholesterol were measured and the participants were then crossed-over to the alternate diet. After two-weeks on the second diet, cholesterol levels were once again recorded.

1.  How was the data collected differently from Example 7.2?

<span style="color:blue;">In Example 7.2, there were 14 different subjects on each diet. Now each subject was on both diets (i.e., SAME subject BETWEEN groups). Thus, the independent observations between groups is violated.</span>

2. Are the cholesterol levels of individuals independent across the 28 measurements? Explain.

<span style="color:blue;">No, there were only 14 subjects and one person's cholesterol on the cornflake diet will be related to the same person's cholesterol on the oatbran diet --> dependent!</span>

Since we can match or pair the cholesterol level measurements between the treatment groups, we call this a **paired study design**. These could be paired up by taking measurements on the same observational unit twice or taking measurements on similar observational units (e.g. identical twins, similar fields, etc.). When analyzing the data and making conclusions, we must take this design into consideration or we are violating a key assumption (independent observations *between* groups!).

The data set below shows the data for each participant `ID`. The `CORNLK` column indicates the cholesterol level for that individual on the corn flake diet and the `OATBRAN` column indicates the cholesterol level for that individual on the oat bran diet.

\vspace{0.1in}

```{r data}
#| echo: true
head(cholesterol_data)
```

+ <span style="color:blue;">Before (2-sample independent t-test): difference in population means $(\bar x_\text{CF}-\bar x_\text{OB})$</span>
+ <span style="color:blue;">Now (paired): population mean of differences $(\bar x_\text{CF-OB})$ -- treat this like we did in Ch. 6!</span>

\vspace{0.1in}

3.	What can be said about the Cholesterol of Subject ID #1 as compared to Subject ID #2, for example, regardless of which diet the Cholesterol levels were measured after?

<span style="color:blue;">Subject #1 appears to have lower cholesterol levels than subject #2, in general.</span>

::: callout
#### Big Idea

For these data, the cholesterol level on the corn flake diet is related to the cholesterol level on the oat bran diet (the two measurements were made on the same person).  Thus, these two samples are *dependent*.

\vspace{0.1in}

In other words, much of the variability in the observations is due to differences between people. So, to control for this variability in cholesterol levels from person to person (which will help us isolate the effect of diet), we will work with the **DIFFERENCES** on each subject, instead.  This will remove the structure of dependence between the corn flake and oat bran groups and will control for the fact that some people, in general, tend to have higher (or lower) cholesterol levels than others.
:::

4. What does the `CholesterolDiff` column represent? Why does this calculation make sense?

<span style="color:blue;">The change in cholesterol levels between the two diets for each subject.</span>

5. What does a positive difference of 0.77 indicate?

<span style="color:blue;">The subject's cholesterol level was 0.77 mmol/L higher on the cornflake diet than the oatbran diet.</span>

6. What does a negative difference of -0.45 indicate?

<span style="color:blue;">The subject's cholesterol level was 0.45 mmol/L lower on the cornflake diet than the oatbran diet.</span>

A histogram of the differences in cholesterol levels for the 14 individuals and a table of summary statistics are shown below. 

<span style="color:blue;">Ehh, not super normal, but let's proceed anyway.</span>

```{r histogram}
#| fig-align: center
#| fig-height: 3
#| fig-width: 4
#| out-width: 75%
ggplot(data = cholesterol_data, 
       mapping = aes(x = CholesterolDiff)) +
  geom_histogram(binwidth = 0.25, color = "white") +
  labs(x = "Difference in cholesterol levels \n(Corn Flake - Oat Bran)",
       y = "Number of Subjects") 
```
Summary statistics for the difference in cholesterol levels between the corn flake diet and oat bran diet are given below:

```{r}
#| echo: true
favstats(~ CholesterolDiff, data = cholesterol_data)
```
7.	What is the mean of the differences for the 14 subjects?  Interpret this value. Does this value seem familiar?

<span style="color:blue;">$\bar x_\text{CF-OB}=\bar x_\text{diff}=0.36$ the observed sample mean of the difference in cholesterol levels between all adults on the cornflake and oatbran diet is 0.36mmol/L</span>

8.	If cereal diet had *no effect* on cholesterol levels, what would you expect the mean of these differences to be?

<span style="color:blue;">We would expect no change in cholesterol level for each subject (i.e., we expect $\bar x_\text{diff}=0$</span>

::: callout
#### Big Idea: paired t-test
Note that these differences are represented by a single column of data. So, instead of viewing this as a problem involving a categorical predictor and a numerical response, you could view this as a problem involving a single numerical variable – the differences! Therefore, the hypothesis testing procedure is exactly the same as the procedure for testing a single population mean we discussed in Chapter 6.

\vspace{0.1in}

That is, the parameter of interest is the true population mean of the differences which we will represent by $\mu_\text{difference}$. 

+ Our best estimate for this parameter is the sample mean of the observed differences.  We’ll call this quantity $\bar x_\text{difference}$.

+ The sample standard deviation of the differences will be denoted by $s_\text{difference}$.
:::

We can carry out the hypothesis test as follows to determine whether cholesterol levels of all adults tend to *differ* between cereal diets.

**Research Question** Researchers are still interested in whether eating corn flakes compared to oat bran had an effect on serum cholesterol levels.

9. Write the parameter of interest in words.

<span style="color:blue;">$\mu_\text{difference}=\mu_\text{CF-OB}:$ The population mean difference in cholesterol levels for all adults on the cornflake diet compared to the oatbran diet.</span>

10. Set up the null and alternative hypotheses in words and symbols.

+ <span style="color:blue;">$H_O: \mu_\text{difference} = 0$</span>
+ <span style="color:blue;">$H_A: \mu_\text{difference} \ne 0$</span>

11. Calculate the T test-statistic:

$$T = \frac{\bar x_\text{difference} - \mu_\text{difference}}{s_\text{difference}/\sqrt{n}}$$

<span style="color:blue;">$$T = \frac{0.36-0}{0.406/\sqrt{14}}=3.31$$</span>

12. Using the t-distribution provided below, show how you would estimate the p-value.

```{r}
#| out-width: 70%
#| fig-align: center
knitr::include_graphics("07-images/tdist-7.3.png")
```

<span style="color:blue;">Draw lines at -3.31 and +3.31 then shade the outskirts.</span>

Similar to Chapter 6, the R code below can be used to conduct the paired t-test:

```{r}
#| echo: true
t_test(x = cholesterol_data,
       response = CholesterolDiff,
       mu = 0,
       alternative = "two-sided")
```

13. State a conclusion in context of the study. Make sure to include proper evidence.

<span style="color:blue;">At at $\alpha = 0.05$, we have evidence to conclude the population mean difference in cholesterol levels for all adults beween the cornflake and oatbran diet is different from 0 mmol/L (T = 3.34, df = 13, p-value = 0.0053).</span>

14. How does your conclusion differ from your conclusion in Example 7.2 when we assumed independent samples?

+ <span style="color:blue;">Recall 7.2: T = 0.947, p-value = 0.352 -> we FTR "not enough evidence" - independent two-sample t-test</span>
+ <span style="color:blue;">Now in 7.3: T = 3.35, p-value = 0.005 -> we R "have evidence - paired t-test</span>

<span style="color:blue;">We found different conclusions, thus it is important to analyze data in the way it was collected. y treating the variability (i.e., SE) differently, we were able to focus on the actual research question rather than the variability due to different subjects.</span>

15. Can we conclude that the diet *caused* the change? Explain.

<span style="color:blue;">Yes, they *randomly assigned the order of diet* to subjects.</span>

::: callout-note
#### Checking the Normality Assumption (CLT) for a Paired t-test

Recall that for the t-test to be valid, at least one of the following conditions must be met:

+ Either the sample size is sufficiently large (greater than 30 or so), or
+ The distribution of the observed data is approximately normal (which would indicate that the population is normally distributed so that the Central Limit Theorem would apply even with a small sample size)
:::

16. Does the t-test appear to be a valid approach for testing this research question?  Justify your reasoning.

<span style="color:blue;">Since n = 14 < 30, the histogram of the observed differences is questionably normal... I plan to proceed anyway. If I was doing this study, I would aim for more subjects.</span>

17. Using the formula for confidence intervals for single numerical variables from Chapter 6 and the t-quantile provided below, calculate the 90\% confidence interval.

```{r}
#| echo: true
qt(0.95, 13)
```

$$\bar x_\text{difference} \pm \text{t-quantile}\big(\frac{s_\text{difference}}{\sqrt{n}}\big)=$$

<span style="color:blue;">$0.36 \pm 1.77(0.406/\sqrt{14})=(0.12, 0.59)$</span>

18. Where does 0 fall within the confidence interval? Why does this make sense?

<span style="color:blue;">The null value of 0 falls outside the confidence interval. Thus, 0 is not a *plausible* value for the population mean difference which makes sense since we rejected the null and found evidence to conclude the population mean difference in cholesterol between cornflake and oatbran was different from 0.</span>
